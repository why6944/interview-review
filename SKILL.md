---
name: interview-review
description: Use when the user wants to create an interview review (面试复盘) document from an interview recording transcript plus a job description (JD). Triggers on requests like "面试复盘", "帮我复盘这次面试", "生成面试复盘文档", "interview review", "复盘文档". Produces a structured retrospective that lists every interviewer question (no omissions), marks each as 【新话题】 or 【追问】 with follow-up chains, scores each answer, generates standard answers plus extended knowledge points (延申知识点) for every weakly-answered question, and turns gaps into an action plan against the JD.
---

# 面试复盘（Interview Review）

## Overview

把一次面试的**录音转文字（逐字稿）+ 岗位 JD**，转成一份结构化的复盘文档。

核心原则只有一条：**复盘的价值不在"记录发生了什么"，而在"指出下次该改什么"。**
每一处失分都要落到「标准回答 + 延申知识点 + 可执行动作」，不停留在"这里答得不好"。

## 输入要求（Required Inputs）

必须同时拿到两类输入，缺一不可；缺哪样就先向用户要，不要凭空编：

1. **转写文本（逐字稿）**——面试的真实问答记录（可含语音识别错误，见"术语还原"）。
2. **岗位 JD**——岗位职责 + 任职要求，是复盘打分的"标尺"。

## 执行步骤（Workflow）

按顺序执行，不要跳步：

### Step 1 — 完整通读输入
- 逐字稿必须**完整读一遍**，不能跳读、不能只扫标题。价值全在"面试官问了什么、候选人怎么答的、卡在哪"。
- 把 JD 的**任职要求**逐条拆出来（通常 4~8 条），后面要逐条对照。

### Step 2 — 抽取面试官问题清单（要全）
- 从逐字稿里抽出**面试官问的每一个问题**，单独成条，**不合并、不遗漏、不挑重点**。
- 覆盖所有环节：开场自我介绍、背景核实、技术问答、实习/规划、反问环节（含面试官穿插问的问题）。
- 同时记录**候选人的回答要点**（只留关键内容，去掉口语重复）。
- 也记录**面试官的非提问信号**：纠正、暗示、定性评价（如"其实也有办法打断他""这个路线跟我们匹配"）——这些往往是复盘里最该警惕的点。

### Step 3 — 标注追问关系（核心）
给每个面试官问题打标签，判断它和上一个问题的关系（规则见下"追问关系判断规则"）。

### Step 4 — 逐题复盘（四件套）
对每个问题统一输出四层：
1. **标签**（【新话题】/【追问·承 Q 几】）
2. **我的回答**（还原现场，如实，不美化）
3. **评分**（三档，见下"评分标准"）
4. **标准回答 + 延申知识点**（告诉用户"正确的是什么 + 下次怎么答"，这是复盘的核心价值）

**硬性规则（回答不好的题）**：评分 ⭐（明显卡壳或错误）或 ⭐⭐（有方向但浅）的问题，必须单独输出「**标准回答**」和「**延申知识点**」两个独立小节，不能只写一句"这里答得不好"：
- **标准回答**：假设面试官再问一次，候选人应该怎么答——写成可直接背诵/口述的完整段落，包含正确术语、API/命令、关键数据，必要时附代码片段。
- **延申知识点**：该题相关的相邻考点（二面最可能顺着追问的下一层问题），逐条列出，每条给"一句话要点"，不做长篇展开；覆盖深度以"能扛住面试官顺着这个方向再追问一轮"为准。
- 评分 ⭐⭐⭐ 的问题也鼓励补"延申知识点"把扎实升级为满分（标准回答可略）。

### Step 5 — 分类
按**知识领域**给题目分组（如 CUDA/GPU、Linux 驱动、内存管理、内核裁剪、OS/多线程、C 语言、处理器经验、背景核实、实习规划等）。分类用于"逐题复盘"的呈现维度，与"脉络总览"的时间序并存。

### Step 6 — 三条线收口
- **亮点线**：哪些答得好、该保持。
- **短板线**：按失分严重度排 P0/P1/P2，明确指出"下一轮最危险的地方"。
- **差距 + 行动线**：对照 JD 逐条看缺口，落到"二面前 / 终面前 / 入职前"的具体清单。
- **结论先行**：把亮点线和短板线收敛成一份「结论先行」，放在文档**最开头**——具体到题号，明确「哪些题答得好、为什么好」「哪些题答得不好、为什么不好、下次怎么避免」。这是复盘文档里用户最该先看的部分。

### Step 7 — 生成文档
按下方"输出文档结构"写出完整 Markdown 复盘文档，保存到约定位置。

## 追问关系判断规则

判断每个问题是【新话题】还是【追问】：

- **【新话题】**＝面试官主动切换到新的知识领域 / 新的问题维度，与上一题不构成深挖关系。
  - 判据：问题里的对象/关键词**不属于上一题所在的知识域**（例：从 GPU 算子跳到 Linux 驱动）。
- **【追问】**＝在同一问题域内，针对上一题的回答继续深挖。
  - 判据：问题**引用了上一题（或更早）回答里出现过的概念**（例：候选人在 Q15 答了"warp"，下一题问"warp 多大"）。
  - 追问的常见类型：
    - **澄清**：「你说是哪个延时？」「以 pthread 的互斥锁为例」
    - **深挖**：「为什么」「会产生什么后果」「必然吗」
    - **换角度逼问**：「从 C 语言的角度出发」「假如没有 flash 呢」
    - **确认/定性**：「之前没用过这个关键字是吧」
    - **扩展同一概念**：「那 block、grid 的层级呢」

**标注格式**：追问必须写清它承接的是哪一个问题，如 `【追问·承 Q15】`。连续的追问构成一条「追问链」（例如 static 关键字 Q46→Q51 六连问），在总览里要能一眼看出这条链。

## 评分标准（三档）

- **⭐⭐⭐ 扎实**：准确、完整、有体系，术语正确，能串起来。
- **⭐⭐ 有方向但浅**：能说出方向，但一问「为什么 / 具体场景 / 完整链路」就卡壳或不够精确。
- **⭐ 明显卡壳或错误**：直接说不会、答错、或需要面试官提示才能答。

## 输出文档结构（模板）

```markdown
# {公司} · {岗位} 一面复盘

> 复盘人 / 复盘日期 / 面试形式 / 面试时长 / 面试官

## 结论先行（先看这个）

### ✅ 回答得好的问题（为什么好）
- 具体到题号，每题一句"好在哪"（框架清晰 / 数据完整 / 底层原理到位 / 诚实应对等）。

### ❌ 回答得不好的问题（为什么不好 + 下次怎么避免）
- 具体到题号，每题两行：① 为什么不好（一句话定性）② 下次怎么避免（可执行动作，不停留于"多练"）。

---

## 一、面试基本信息（表格：岗位 / 公司 / 部门业务 / 轮次 / 后续流程 / 面试官口径）

## 二、总体评价
一句话结论 + 亮点 ✅ + 短板 ⚠️（3~4 条）

## 三、自我介绍复盘
现场结构 → 评价 → 可优化点

## 四、面试官提问脉络总览（时间序 · 追问关系全标记）
按阶段分组，表格三列：# | 问题 | 类型（【新话题】/【追问·承 Q 几】/【提示】）

## 五、技术问答逐题复盘（分类 · 含追问标记）
按知识领域分组（A/B/C/...），每题：
**A{序号}（=Q{全局序号}）{问题}【类型】** {评分}
- 我的回答：...
- 评价：...
- 标准回答：...（⭐⭐ 及以下必写，可直接背诵口述）
- 延申知识点：...（⭐⭐ 及以下必写，逐条一句话要点）

## 六、表现亮点（值得保持）

## 七、短板与知识盲区（按 P0/P1/P2 排序，表格含"对应题号"）

## 八、对照 JD 的差距分析（逐条任职要求 → 现状 → 差距 → 补强建议）

## 九、部门与流程信息（从面试中获取）

## 十、后续行动清单（短期/中期/长期，可执行）

## 附：面试官原话关键信号
```

## 术语还原

逐字稿常有语音识别错误，复盘时要**还原成正确术语**，但存疑处必须标注：
- 常见误识别示例：「扩展分子」→ 算子、「库达/加尔库达」→ CUDA、「优布T」→ U-Boot、「福特四向量化」→ float4 向量化、「S、MD/SMT」→ SIMD/SIMT、「ATM32」→ STM32、「升腾木希」→ 昇腾、沐曦。
- 无法确定的人名/型号（如某个"62"芯片）→ 照实保留原文并标 `（转录不清，存疑）`，不要替用户编一个正确名词。

## 诚实原则（Hard Rules）

- 复盘是给候选人自己看的，**如实记录"答得不好/答错/说不会"**，不美化、不替用户圆。
- **不编造**候选人没说过的话、没做过的项目；候选人说"是师兄做的/拿来学习"的，要如实标为"学习"性质。
- 面试官给出的负面信号（提示、纠正、"差点意思"）要明确点出，不能淡化。

## 输出位置约定

- 默认保存到**逐字稿同目录**，文件名 `<原文件名>_复盘.md`。
- 用户指定位置时以用户为准。

## Announce

开始执行时先说明："我来用 interview-review skill 生成面试复盘文档。"
